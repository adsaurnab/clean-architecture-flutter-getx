class AppConstant {
  // API Details
  static const apiBaseUrl = 'https://api.example.com';
  static const connectionTimeout = Duration(seconds: 30);

  // App Strings
  static const appName = 'My Awesome App';
  static const welcomeMessage = 'Welcome back!';

  // Numeric Layout Metrics
  static const defaultPadding = 16.0;
  static const borderRadiusMedium = 8.0;

  // ── UI ────────────────────────────────────────────────────────────────────
  static const double borderRadius = 12.0;
  static const double cardElevation = 2.0;
  static const Duration animDuration = Duration(milliseconds: 250);
  static const double avatarSizeSmall = 36.0;
  static const double avatarSizeMedium = 48.0;
  static const double avatarSizeLarge = 72.0;
}
